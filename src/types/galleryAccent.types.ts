export type GalleryAccent = {
  /** 列表相框描边色 */
  accent: string;
  /** 外发光 */
  glow: string;
  /** 实色（蝴蝶结 / 流光） */
  solid: string;
  /** 浅色高光 */
  soft: string;
  /** 深色结心 / 爱心 */
  deep: string;
};
